<!-- hAIve bridge file — do not edit by hand. -->
<!-- This file points your AI tool at the shared hAIve project context. -->

See `.ai/project-context.md` for the full project context.
Memories live under `.ai/memories/` (personal/team/module).

<!-- haive:memories-start -->
<!-- AUTO-GENERATED by haive sync --inject-bridge — do not edit between these markers -->

### 2026-04-25-architecture-embedderlike-interface (team/architecture)
Use the EmbedderLike interface (model, dimension, encode) instead of the concrete Embedder class anywhere downstream (indexer, semanticSearch). This lets tests inject a deterministic FakeEmbedder without downloading the 110MB bge-small model.

---

### 2026-04-25-architecture-pure-tool-handlers (team/architecture)
MCP tool implementations under packages/mcp/src/tools/ are pure async functions that take (input, ctx) and return a JSON-serializable result. The McpServer in server.ts is a thin wrapper that registers them with the SDK. This lets us unit-test handlers without going through stdio.

---

### 2026-04-25-convention-always-run-tests-before-commit (team/convention)
Personal habit: pnpm test from the root before any commit. Catches the cross-package breakage that local watch mode misses.

---

### 2026-04-25-convention-use-pnpm-workspaces (team/convention)
This monorepo uses pnpm workspaces (not npm/yarn workspaces, not turborepo). Run pnpm install at the repo root to install everything. Run pnpm -r build / pnpm -r test to act on every package. Cross-package deps use 'workspace:*'.

---

### 2026-04-25-decision-esm-only (team/decision)
All packages are ESM-only (type: module in package.json, format: esm in tsup). Imports must use .js extensions even for .ts source files (verbatimModuleSyntax + isolatedModules in tsconfig). Do not add CJS output.

<!-- haive:memories-end -->
