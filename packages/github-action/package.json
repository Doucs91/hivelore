{
  "name": "@hivelore/github-action",
  "version": "0.4.5",
  "description": "GitHub Action for Hivelore context policy and decision enforcement on pull requests",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Doucs91/hivelore.git"
  },
  "scripts": {
    "build": "tsup src/run.ts --format cjs --out-dir dist --no-splitting --minify",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.3",
    "tsup": "^8.3.5",
    "vitest": "^2.1.9"
  }
}
