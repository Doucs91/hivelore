{
  "name": "@hiveai/github-action",
  "version": "0.4.4",
  "description": "GitHub Action that surfaces hAIve team memories on pull requests",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/Doucs91/hAIve.git"
  },
  "scripts": {
    "build": "tsup src/run.ts --format cjs --out-dir dist --no-splitting --minify",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.3",
    "tsup": "^8.3.5"
  }
}
