node_modules
dist
*.log
.DS_Store
.env
.env.local
coverage
.vitest-cache
.tsbuildinfo

# hAIve local cache (embeddings index, model weights, etc.)
.ai/.cache/
.ai/.runtime/
.ai/memories/personal/

# hAIve project-level MCP configs generated per machine (absolute paths)
.cursor/mcp.json
.vscode/mcp.json
.mcp.json

# Test projects — not part of the repo
sandaga-monorepo/
engram/

# Benchmark fixtures — npm install generates locally

# SonarQube MCP (secrets + certificats locaux)
sonar-mcp.local.env
sonar-mcp-ca/
# hAIve project-level MCP configs (machine-specific absolute paths)
.ai/.cache/*
!.ai/.cache/.gitignore
.ai/.runtime/*
!.ai/.runtime/.gitignore
!.ai/.runtime/README.md
benchmarks/agent-benchmark/
benchmarks/agent-benchmark-rework/
benchmarks/.tpl/
benchmarks/.accept/
benchmarks/RESULTS.md
benchmarks/manual-run/fixtures/**/package-lock.json
